# general_template for MLflow application
general template for any MLflow project

conda create  --prefix ./env python=3.7 -y

conda activate ./env

```bash
source activate ./env

```

pip install -r requirements.txt

conda penv export > conda.yaml

